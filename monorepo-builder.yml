parameters:
    # for "merge" command
    data_to_append:
        require-dev:
            orchestra/testbench: ^4.4,
            phpunit/phpunit: '^8.4'
            symplify/monorepo-builder: '^7.1'

    # for "split" command
    directories_to_repositories:
        packages/laravel: 'git@github.com:reed-jones/vuexcellent-laravel.git'
