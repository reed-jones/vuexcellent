parameters:
    # for "merge" command
    data_to_append:
        require-dev:
            phpunit/phpunit: '^7.3'
            symplify/monorepo-builder: '^7.1'

    # for "split" command
    directories_to_repositories:
        packages/first-package: 'git@github.com:reed-jones/test-package.git'
